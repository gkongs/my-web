import { DefaultTheme } from 'styled-components';
import { Colors } from './variables';

export const theme: DefaultTheme = {
  primary: { main: '#FAF0E6' },
  secondary: { main: '#FFE4E1' },
  black: Colors.black,
  white: Colors.white,
};
